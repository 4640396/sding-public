# 知识库协作规则

## 定位

这是一个以 Obsidian Markdown 为唯一事实来源的个人知识库。Agent 可以读取、整理和提出修改建议，但不得把检索索引或模型生成内容当作事实来源。查询、分析、审查和总结默认只读；只有用户明确要求记录、同步、沉淀、修改或实施时，才创建、编辑或移动 Vault 文件。

## 读取顺序

1. 从 `主页.md` 和 `书架/书架索引.md` 判断入口。
2. 项目问题先读取 `书架/06 项目/项目索引.md`，由该索引定位具体项目索引，再按任务进入对应模块笔记。
3. 只有 `status: verified` 笔记中明确标出的事实和验证结果可作为稳定知识；建议、计划和未验证边界不会因所在笔记为 `verified` 自动成为事实。其他状态只作为对应状态的证据或上下文。
4. 笔记冲突时先比较状态和来源权威性；在同为 `verified` 的材料之间，优先采用来源明确且更新时间较新的内容。必须指出冲突，无法判定时保留不确定性，不自行合并成新事实。

## 目录语义

- `00 收集箱`：刚收到、尚未分类或尚未开始处理的材料，不视为已验证知识。
- `01 工作台`：正在分析、编辑、排查或等待确认的内容，可能不完整。
- `02 日记`：按日期记录的上下文和流水。
- `03 卡片盒`：可复用的原子知识。
- `04 知识地图`：导航和关系入口。
- `05 领域`：长期主题知识。
- `06 项目`：需要长期追溯的项目工件；可以包含不同状态，但只有 `verified` 内容可作为稳定事实。
- `07 资源库`：跨项目复用的模板、资料、工具和素材。
- `08 实验室`：实验过程与结果。
- `99 归档`：不再活跃、仅供追溯的内容。

项目索引留在项目根目录；其他长期项目工件按需使用 `01 需求与决策`、`02 架构与实现`、`03 实施与验收`、`04 运维与迁移`、`05 历史与复盘`。分类有内容时再创建，不建立空目录。

## 写入规则

- 刚收到且尚未处理的材料写入 `书架/00 收集箱`；正在处理或 Agent 生成、等待确认的内容写入 `书架/01 工作台/<项目名>/`。
- 只有用户明确要求沉淀，且内容已经形成完整、需要长期追溯的项目工件时，非 `verified` 内容才可按其真实状态进入项目目录。
- 未经用户明确要求，不直接覆盖稳定项目说明、移动稳定笔记或删除历史记录。
- 新笔记使用 YAML Frontmatter，至少包含 `title`、`type`、`status`、`created`、`updated` 和 `sensitivity`；稳定结论还必须使用 `sources` 记录来源。
- Markdown 正文统一采用 Obsidian 默认阅读样式：每篇笔记只使用一个一级标题，再按二级、三级标题组织段落、列表、代码块和 Wiki 链接。不得使用 `cssclasses`、自定义布局 Callout 或 HTML 标签搭建页面；`note`、`warning`、`important`、`summary` 等标准 Callout 只在确有语义需要时使用。
- Vault 内的 SQL 内容统一写入 Markdown 笔记，并使用标注语言的 `sql` fenced code block；不在 Vault 中单独保存 `.sql` 文件。若需执行，复制代码块内容到数据库客户端；项目源码仓库中的迁移脚本不受此规则影响，只在笔记中记录其来源路径和验证结果。
- 数据比对、转换、生成和校验等可执行脚本属于项目代码工件，必须移动到来源项目的仓库或项目级 `tools/<任务名>` 目录；不得放进 Vault，也不得长期留在日期化输出目录。输出目录只保留交付物和验证证据。Agent 应在授权范围内直接移动脚本、修正引用并验证，不要求用户再次手工复制，也不得保留两个当前副本。
- `draft` 表示未确认分析或方案；`accepted` 表示已接受但不代表已实施；`verified` 只保证明确标出的事实和验证结果；`backlog` 表示以后处理或尚未决定；`reference` 表示参考或历史证据；`deprecated` 表示已被替代，并使用 `superseded_by` 指向替代项。
- 不得仅因移动、整理或 Agent 生成内容而提升 `status`。被替代的内容不直接删除。

## 安全边界

- 不在回答、摘要或新笔记中暴露密码、令牌、Cookie、私钥、真实个人数据或生产数据库连接信息。
- `sensitivity: public` 可按任务授权使用允许的处理后端；`sensitivity: internal` 只允许本地处理；`sensitivity: confidential` 只能在本地、当前任务必要范围内读取，永不进入索引或外部服务。
- 缺少 `status` 或 `sensitivity` 的笔记不得进入稳定索引。
- `.obsidian/`、收集箱、工作台、日记、资源库、实验室、归档、Excel 和其他白名单之外的内容默认不进入索引；不得仅凭用户的一般性整理授权扩大索引范围。

## GBrain 索引底线

- GBrain 只生成 Vault 外的只读、可删除、可重建索引镜像，不自动回写 Vault。
- Graphify 代码图谱与缓存默认写入 `%LOCALAPPDATA%/Graphify/projects/<project-id>/graphify-out`；不得在 Vault 或源码仓库中生成。调用时通过绝对 `GRAPHIFY_OUT` 环境变量或显式 `--graph` 路径定位。
- 稳定索引只接受 `书架/03 卡片盒`、`书架/04 知识地图`、`书架/05 领域`、`书架/06 项目`内同时满足 `status: verified` 与 `sensitivity: public|internal` 的 Markdown。
- Graphify 只作为辅助关系索引；重要结论必须回到原始 Markdown 核验。
- 检索答案应返回来源笔记，说明状态、冲突和不确定性。
