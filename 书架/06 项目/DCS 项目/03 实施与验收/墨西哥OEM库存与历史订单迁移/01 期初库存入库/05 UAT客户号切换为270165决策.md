---
title: MX迁移UAT客户号切换为270165决策
type: decision
status: accepted
created: 2026-07-22
updated: 2026-07-22
sensitivity: internal
project: DCS
sources:
  - 用户确认 UAT 当前已使用客户号 270165（2026-07-22）
  - "[[01 单车验收与修订结论]]"
  - "[[03 批量导入执行方案]]"
  - "[[04 生产数据比对与批次模板准备记录]]"
---

# MX迁移UAT客户号切换为270165决策

## 当前决策

用户于 2026-07-22 确认：UAT 当前已使用客户号 `270165`。墨西哥 OEM 采购迁移的现行 UAT 参数与 PRD 对齐：

```text
订单 Sheet DEALER SAP NO = 270165
发运 Sheet 客户编号 = 270165
发运 Sheet 送达方编号 = 270165
JSON customerNo = 270165
JSON deliveryItemVos[].customerNumber = 270165
```

## 替代关系

2026-07-18 单车 POC 使用 `220202` 成功，是当时环境的已验证历史事实，不删除、不改写。该旧映射不再作为当前批量执行参数。

以下旧工件包含 `220202`，仅保留为历史证据，禁止继续执行：

- 文件名或目录名包含 `UAT_220202` 的采购发运模板；
- `generation_summary_uat_220202.json`；
- `verification_summary_uat_220202.json`；
- 依赖上述旧映射生成的 JSON 或接口请求体。

## 执行要求

1. 当前 UAT 执行必须使用 `270165` 版本模板，或从旧模板重新生成并复验三个组织字段。
2. 不得仅修改文件名；必须检查 Excel 和转换后 JSON 的实际字段值。
3. 已使用 `220202` 导入的历史 UAT 数据不自动视为已迁移到 `270165`；是否清理、重导或保留必须依据 UAT 数据库现状单独决策。
4. 本决策来自用户对当前环境的确认，尚未附 UAT 主数据查询或接口验收截图，因此状态为 `accepted`，不是 `verified`。
